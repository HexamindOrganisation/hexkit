/**
 * Chat page — Phase F0 stub.
 *
 * Renders a placeholder until Phase F3 wires up <AgentUI> + the runtime
 * bridge. Verifying F0 just needs the route to exist and resolve under
 * the auth guard.
 */
export function ChatPage() {
  return (
    <div className="flex h-full items-center justify-center p-8 text-sm text-muted-foreground">
      Chat coming online in Phase F3. Pick an agent above; pick a conversation
      in the sidebar.
    </div>
  );
}
